{
  "name": "midnight-rpc-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker reverse-proxy for Midnight RPC, bypassing the AWS ELB cloud-IP block on rpc.preprod.midnight.network. Pairs with midnight-ops-doctor.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
